import 'package:flutter/material.dart';

class ProjectExpense extends StatelessWidget {
  const ProjectExpense({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
